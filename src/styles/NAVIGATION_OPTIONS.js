import GLOBALS from '../globalVariables';

export default {
    HEADER: Object.assign({
        headerStyle: {
            backgroundColor: 'white'
        },
        headerTintColor: GLOBALS.COLOR.BLUE,
        headerTitleStyle: {
            color: GLOBALS.COLOR.BLUE,
            textAlign: 'center',
            fontSize: 24,
            fontWeight: 'bold'
        }
    })
}